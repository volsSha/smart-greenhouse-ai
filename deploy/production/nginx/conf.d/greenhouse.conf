upstream greenhouse_app {
    server app:8080;
}

server {
    listen 80;
    server_name greenhouse.volsh.dev;

    location = /api/health/live {
        proxy_pass http://greenhouse_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name greenhouse.volsh.dev;

    ssl_certificate /etc/nginx/certs/greenhouse.volsh.dev/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/greenhouse.volsh.dev/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    auth_basic "Smart Greenhouse";
    auth_basic_user_file /etc/nginx/secrets/nginx.htpasswd;

    location = /api/health/live {
        auth_basic off;
        proxy_pass http://greenhouse_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location / {
        proxy_pass http://greenhouse_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }
}
